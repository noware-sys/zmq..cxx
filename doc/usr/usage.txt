Simply #include the required files, without any extension.
That is all which should normally be necessary.

Each folder should contain a file named '*' which should include all relevant files in that folder.

Examples:
	#include <zmq/msg>

